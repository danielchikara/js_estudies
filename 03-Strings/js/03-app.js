const producto = "monitor de: ";
const pulgadas = "24 ";
// concatenacion 
console.log(producto.concat(pulgadas));
console.log(producto+pulgadas);
console.log(`El prodcuto ${producto} es de un tamaño de ${pulgadas}`) 