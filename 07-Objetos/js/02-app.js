const name = "monitor de  pulgadas";
const price = 300;
const disponible = true;

const  producto = {
    name: name,
    price: price,
    disponible: disponible
}

console.log(producto);

// maneras de acceder al objeto
console.log(producto.name);
console.log(producto['name']);

