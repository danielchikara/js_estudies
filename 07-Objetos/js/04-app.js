let disponible = true;

let  producto = {
    name: "monitor de  pulgadas",
    price: 300,
    disponible: disponible
}

// extraer de un objeto

let {name,price} = producto;

console.log(name)
console.log(price)
